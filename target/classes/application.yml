spring:
  data:
    mongodb:
      host: "localhost"
      port: 27017
      database: "admin"
      username: "root"
      password: "1234"
      authentication-database : "admin"
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: "DEBUG"